# Base de datos
DB_URL=jdbc:postgresql://localhost:5432/quedate
DB_USERNAME=postgres
DB_PASSWORD=change-me

# JWT
JWT_SECRET=change-me-generate-a-secret-of-at-least-32-characters
JWT_EXPIRATION=3600000
REFRESH_EXPIRATION=604800000

# SMTP (Brevo / Resend, se conecta en deploy)
SMTP_HOST=smtp-relay.brevo.com
SMTP_PORT=587
SMTP_USERNAME=change-me
SMTP_PASSWORD=change-me
SMTP_FROM=no-reply@quedate.com

# CORS
CORS_ALLOWED_ORIGINS=http://localhost:3000